export CC := $(TARGETARCH)-gcc
export CXX := $(TARGETARCH)-g++
export AR := $(TARGETARCH)-ar
export AS := $(TARGETARCH)-as
export RANLIB := $(TARGETARCH)-ranlib
export LD := $(TARGETARCH)-ld
export STRIP := $(TARGETARCH)-strip
