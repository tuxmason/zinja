--- libatomic_ops-7.6.2/ltmain.sh.orig	2018-03-07 18:45:25.749944461 +0300
+++ libatomic_ops-7.6.2/ltmain.sh	2018-03-07 18:46:30.140725701 +0300
@@ -8361,7 +8361,7 @@
 	      fi
 	    else
 	      # We cannot seem to hardcode it, guess we'll fake it.
-	      add_dir=-L$libdir
+	      add_dir="-L$lt_sysroot$libdir"
 	      # Try looking first in the location we're being installed to.
 	      if test -n "$inst_prefix_dir"; then
 		case $libdir in
